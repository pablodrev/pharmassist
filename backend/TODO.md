<!-- 1. Я вижу что при анализе (например POST /api/v1/reports) очень много запросов к huggingface. Они идут при каждом запросе. В чем их цель и есть ли необходимость? Если да, то может быть есть простой метод оптимизации? -->

<!-- 2. Сейчас при отправаке запроса на анализ (текстом POST /api/v1/report или формой POST /api/v1/form-form или файлом POST /api/v1/extract-from-file) ответ не приходит до тех пор, пока весь анализ не будет выполнен. Это занимает долго - около 1 минуты. Нужно чтобы запрос отправлялся асинхронно и ответ получался тоже асинхронно. Какие есть варианты? -->
<!-- 3. В чем отличие анализа кейсов из текста и из заполненной формы для LLM? -->
<!-- 3. Как происходит анализ если в документах нет инструкции к искомому препарату? Сложно ли будет сделать автоматическое добавление документа с инструкцией (парсинг с сайта)? -->

<!-- 4. Убери поле "case_narrative" при анализе из файла и проверь где он еще есть. Его не нужно генерировать, потому что он практически всегда совпадает с полным описанием кейса и тратит токены впустую из-за этого.
6. Как сейчас реализуется анализ через файл? Я вижу, что эндпоинт только извлекает текст, но не выполняет весь пайплайн. Проверь, выполняется ли сейчаас весь анализ? Если нет, Какие есть варианты?
7. RAG не позволяет добавлять довые документы. При запросе POST /api/v1/rag/documents ошибка "pharma-backend | File "/app/api/routes/rag.py", line 72, in upload_document
         pharma-backend | rag.add_document(doc_id, text, {"drug_name_ru": drug_name_ru})
         pharma-backend | ^^^^^^^^^^^^^^^^
         pharma-backend | AttributeError: 'RAGEngine' object has no attribute 'add_document'" -->
